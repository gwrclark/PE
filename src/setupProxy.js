const gwProxySetupFn = require('gw-build-proxy-setup').proxySetup;

/* setupProxy.js is by convention in every application that needs to setup a proxy for development
 * this proxy is maintained centrally, but you customize it here if you need to
 */

module.exports = gwProxySetupFn;
